- config:
    title: 'Map micro: list-populated'
    target: contacts
    platform: android
    driver: ANDROID_ONDEVICE_ACCESSIBILITY
    tags:
    - map-micro
- prompts:
  - step: Navigate to the list-populated state
    recording:
      tools:
      - launchApp:
          appId: com.android.contacts
          launchMode: RESUME
